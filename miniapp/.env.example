# PayDay 微信小程序环境变量配置模板
#
# 使用方法：
# 1. 复制此文件为 .env.development 或 .env.production
# 2. 填写实际的配置值
# 3. .env.* 文件已在 .gitignore 中，不会被提交到代码仓库

# =============================================================================
# API 配置
# =============================================================================
# 后端 API 地址
# 开发环境：VITE_API_BASE_URL=http://localhost:8000
# 生产环境：VITE_API_BASE_URL=https://your-domain.com
VITE_API_BASE_URL=http://localhost:8000

# 注意：已移除客户端 API 签名功能
# 现在使用 HTTPS + JWT 认证，更安全且不需要在客户端暴露密钥

