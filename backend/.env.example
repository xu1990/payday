# 薪日 PayDay 主服务环境变量示例
#
# 使用方法：
# 1. 复制此文件为 .env：cp .env.example .env
# 2. 填写实际的配置值
# 3. .env 文件已在 .gitignore 中，不会被提交到代码仓库
# 4. 生成安全密钥：python scripts/generate_secrets.py

# =============================================================================
# 应用配置
# =============================================================================
APP_NAME=薪日 PayDay
DEBUG=false

# =============================================================================
# MySQL 数据库（技术方案 2.2）
# =============================================================================
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=payday
MYSQL_PASSWORD=your_secure_password_here
MYSQL_DATABASE=payday_main

# =============================================================================
# Redis 缓存（技术方案 2.3）
# =============================================================================
REDIS_URL=redis://127.0.0.1:6379/0

# =============================================================================
# 微信小程序配置（技术方案 2.1）
# =============================================================================
WECHAT_APP_ID=your_wechat_app_id
WECHAT_APP_SECRET=your_wechat_app_secret

# 微信支付配置
WECHAT_MCH_ID=your_merchant_id
WECHAT_PAY_API_KEY=your_pay_api_key
WECHAT_PAY_NOTIFY_URL=https://your-domain.com/api/v1/payment/notify

# =============================================================================
# JWT 认证配置（技术方案 2.1）
# =============================================================================
# 安全提示：JWT_SECRET_KEY 必须至少 32 字节
# 生成方法：python -c "import secrets; print(secrets.token_urlsafe(43))"
JWT_SECRET_KEY=change-me-in-production-use-openssl-rand-hex-32
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=10080

# =============================================================================
# 金额加密配置（技术方案 2.2.3）
# =============================================================================
# 安全提示：ENCRYPTION_SECRET_KEY 必须是 32 字节的 URL-safe base64 编码密钥
# 生成方法：python -c "import secrets; print(secrets.token_urlsafe(43))"
ENCRYPTION_SECRET_KEY=change-me-32-bytes-url-safe-base64-key

# =============================================================================
# API 请求签名密钥（可选，用于小程序请求验证）
# =============================================================================
# 注意：签名验证现在是可选的，主要用于向后兼容
# 如不需要可留空，建议使用 JWT 认证即可
API_SECRET=

# =============================================================================
# CORS 配置（技术方案 安全）
# =============================================================================
# 多个源用逗号分隔
#
# ⚠️ 安全提示：
# - 开发环境：可以使用 localhost 和 127.0.0.1
# - 生产环境：必须设置具体的域名，不要使用通配符 '*' 或 localhost
#
# 开发环境示例：
# CORS_ORIGINS=http://localhost:5174,http://127.0.0.1:5174
#
# 生产环境示例（请替换为你的实际域名）：
# CORS_ORIGINS=https://admin.yourdomain.com,https://yourdomain.com
CORS_ORIGINS=http://localhost:5174,http://127.0.0.1:5174

# =============================================================================
# 腾讯云 COS 对象存储（技术方案 1.2.2）
# =============================================================================
COS_SECRET_ID=your_cos_secret_id
COS_SECRET_KEY=your_cos_secret_key
COS_REGION=ap-guangzhou
COS_BUCKET=payday

# =============================================================================
# 阿里云 OSS 对象存储（备选方案）
# =============================================================================
OSS_ACCESS_KEY_ID=your_oss_access_key_id
OSS_ACCESS_KEY_SECRET=your_oss_access_key_secret
OSS_ENDPOINT=oss-cn-hangzhou.aliyuncs.com
OSS_BUCKET=payday

# =============================================================================
# 存储服务选择
# =============================================================================
# 可选值: cos（腾讯云） | oss（阿里云）
STORAGE_PROVIDER=cos

# =============================================================================
# 腾讯云天御内容安全（可选）
# =============================================================================
TENCENT_SECRET_ID=your_tencent_secret_id
TENCENT_SECRET_KEY=your_tencent_secret_key
TENCENT_REGION=ap-guangzhou

